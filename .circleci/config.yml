version: 2
jobs:
    build:
        docker:
            - image: circleci/node:10.13.0
        steps:
            - checkout
            - run: npm install
            - run: npm run test:unit -- --reporter mocha-junit-reporter
            - store_test_results:
                path: ./test-results.xml
